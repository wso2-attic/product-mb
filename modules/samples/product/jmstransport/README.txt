This sample shows how to invoke an in-only and in-out service operations using jms transport. It generates the code using the
given wsdl file which contains the end point details.

To run this sample
1. Start the server
3. Come back to this folder and run ant (defualt target is set to run)
